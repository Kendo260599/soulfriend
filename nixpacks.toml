[phases.setup]
nixPkgs = ["nodejs-20_x", "npm-10_x"]

[phases.install]
cmds = ["cd backend && npm ci --include=dev"]

[phases.build]
cmds = ["cd backend && npm run build"]

[start]
cmd = "node backend/dist/index.js"

[variables]
NODE_ENV = "production"