{
  "name": "soulfriend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm ci --legacy-peer-deps && npm run build",
    "build": "cd frontend && npm run build",
    "install": "cd frontend && npm ci --legacy-peer-deps"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
