[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildCommand = "docker build -t soulfriend ."
watchPatterns = ["backend/**", "Dockerfile"]

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

