[build]
builder = "nixpacks"
nixpacksConfig = { providers = ["nodejs"], variables = { NODE_VERSION = "20" } }

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[environments.production.variables]
NODE_ENV = "production"
PORT = "5000"

